{
  "name": "admin",
  "description": "Configure game-server platform administration",
  "type": 1,
  "default_member_permissions": "32",
  "options": [
    {
      "type": 1,
      "name": "access",
      "description": "Choose which Discord roles may use platform commands"
    },
    {
      "type": 1,
      "name": "repair-card",
      "description": "Recreate or refresh a session's public card",
      "options": [
        {
          "type": 3,
          "name": "session",
          "description": "Choose a session or enter its exact slug",
          "required": true,
          "autocomplete": true
        }
      ]
    }
  ]
}
